terraform console

terraform init

terraform workspace new aws-lab

terraform plan -out=aws-lab.tfplan

terraform workspace list

terraform workspace new aws-lab

terraform apply


terraform init -backend-config="access_key=<your access key>" -backend-config="secret_key=<your secret key>"

#configure aws credentials as ENV
export AWS_DEFAULT_REGION=
export AWS_ACCESS_KEY_ID=
export AWS_SECRET_ACCESS_KEY=
terraform refresh

#configure aws creds through aws cli on terraform file under provider section
shared_credentials_file = "/path/.aws/creds"        or set profile = "default"

